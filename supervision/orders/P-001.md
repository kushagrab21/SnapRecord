PACKET P-001 — Establish the SnapRecord working directory, development memory, and the two feasibility proofs the whole product depends on

**Project:** SnapRecord (working name) — photograph a physical object or its label with a phone camera and turn it into a validated, stored, structured record. A NEW project, separate from Followthrough. Do not modify anything under `~/Desktop/Demo_AI`.
**Issued:** 2026-09-12 by the supervisor. **Target close:** 60 minutes from receipt.

## Context
The supervisor knows the machine from the Followthrough project (Node 26 at `/opt/homebrew/bin`, working OpenRouter and Anthropic keys in `~/Desktop/Demo_AI/.env`, git available) but knows NOTHING about this project's directory, and nothing has been proven about (a) a phone camera reaching a page served from this laptop, or (b) any vision-capable model returning JSON that validates against a schema. Every later packet — capture UI, extraction pipeline, storage, review screen — is built on those two facts, so this packet establishes them honestly before any product code exists. It must NOT build product features and must NOT treat a design note as a working capability.

## Deliverable
A committed repository at `~/Desktop/SnapRecord` containing `supervision/ORIENTATION.md`, `supervision/RESULTS.md`, `supervision/orders/P-001.md`, `supervision/reports/P-001.md`, `supervision/evidence/`, and a returned report that states (a) exactly what exists in the directory and the runtime baseline, (b) which credentials are present by presence only, (c) whether a phone browser can capture a photo and deliver it to a process on this laptop, with the evidence, (d) whether a vision model returns schema-valid JSON for a real photo of a labelled object, with the evidence and cost, and (e) a recommended first product packet.

## Instructions
1. **Directory.** If `~/Desktop/SnapRecord` does not exist, create it; if it exists, describe its contents and do not delete or overwrite anything. `pwd`, top-level listing, git state. Quote any repository instruction file (CLAUDE.md, AGENTS.md, README) verbatim, or state none exists.
2. **Runtime baseline.** `node --version`, `npm --version`, `python3 --version`, `git --version`, `sqlite3 --version` (or absent), whether `npx` works, OS version, free disk. Record the laptop's LAN IP address(es) as reported by the OS (`ipconfig getifaddr en0` or equivalent).
3. **Credentials by PRESENCE ONLY.** Create `.env` (mode 600, git-ignored) and a committed `.env.example` with empty values. Copy from `~/Desktop/Demo_AI/.env` only the keys this project needs: `OPENROUTER_API_KEY`, `ANTHROPIC_API_KEY`, and any `LLM_*` selector. Report each as PRESENT or ABSENT. Never print values, prefixes, or lengths of secrets, and never paste the Followthrough `.env` in any form.
4. **Proof A — phone camera to laptop.** Write `scripts/proof-capture.mjs`: a dependency-free Node HTTP server on `0.0.0.0` at a port of your choice that serves one page containing `<input type="file" accept="image/*" capture="environment">` and a submit that POSTs the image to `/upload`; the server writes each upload to `supervision/evidence/captures/<timestamp>.jpg`, logs byte size, content type, and the client's user-agent, and prints the URL the phone should open (using the LAN IP from step 2). Run it, then STOP and print a single plain sentence asking the user to open that URL on their phone and photograph any labelled object (a packaged food item, a bottle, a book spine — their choice). Wait for the upload. If the phone cannot reach the laptop, record the exact symptom (timeout, refused, certificate) and try the ONE fallback of a plain-HTTP `file` input before concluding; do not install tunnelling tools. Record: whether the `capture` attribute opened the camera directly or a picker, image dimensions (read with `sips -g pixelWidth -g pixelHeight` or equivalent), byte size, and whether EXIF orientation is present (`sips -g orientation` or `exiftool` if available; else state unknown).
5. **Proof B — vision model to schema-valid JSON.** Write `scripts/proof-extract.mjs` that sends the photo from Proof A (base64, downscaled to max 1568px on the long edge if larger — record before/after sizes) to ONE vision-capable model through the key that is PRESENT (prefer OpenRouter; state which model id you used and why), with an instruction to return ONLY JSON matching this schema — copy it verbatim into `schema/item.v0.json`:
   ```json
   {
     "type": "object",
     "required": ["item_name", "category", "label_text", "attributes", "confidence"],
     "properties": {
       "item_name":  { "type": "string" },
       "brand":      { "type": ["string", "null"] },
       "category":   { "type": "string" },
       "label_text": { "type": "string", "description": "all legible text on the label, verbatim" },
       "attributes": { "type": "array", "items": { "type": "object", "required": ["key", "value"], "properties": { "key": {"type":"string"}, "value": {"type":"string"}, "unit": {"type":["string","null"]} } } },
       "quantity":   { "type": ["number", "null"] },
       "confidence": { "type": "number", "minimum": 0, "maximum": 1 },
       "uncertain_fields": { "type": "array", "items": { "type": "string" } }
     },
     "additionalProperties": false
   }
   ```
   Validate the response in code (a hand-written check of required keys and types is acceptable; do not install a validator yet). Run it THREE times on the same image. Record for each run: raw response (verbatim, in evidence), validation pass/fail with the failing key if any, prompt tokens, completion tokens, elapsed ms, and USD cost from the provider's usage/generation endpoint or its published price — say which. Record whether `label_text` matches what is actually printed on the object; the USER must confirm this — ask them in one sentence, and record their answer verbatim as the user's statement, not yours.
6. **Development memory.** Create `supervision/ORIENTATION.md` (~400 words max: directory address, stack decision pending, credentials present/absent, what Proof A and Proof B established, open questions, next step), `supervision/RESULTS.md` (append-only ledger; ID scheme `R-0001`…, collision check by `grep` before appending; one entry per established fact from this packet, each with Claim / Evidence / Caveat), `supervision/orders/P-001.md` (this order, verbatim), `supervision/reports/P-001.md` (your report), `supervision/evidence/` (server log, three raw model responses, the capture image(s), the `sips` output).
7. **Git.** `git init` on branch `main`; `.gitignore` excludes `.env*` except `.env.example`, `node_modules/`, `data/`, `*.sqlite*`, and `supervision/evidence/captures/` (photos of the user's belongings stay local). Commit with a message beginning `P-001:`. Report the hash.
8. **Recommendation.** From evidence only, recommend the first product packet: whether the capture page can go straight to a PWA served from this laptop, whether the schema needs fields added or removed based on what the model actually returned, and which model to standardise on for extraction at what cost per photo. Flag any prerequisite user action (HTTPS for `getUserMedia`, a phone on the same Wi-Fi, account top-ups) as a single plain sentence each.

## Boundaries
No product code, no framework installation (no React, Vite, Express, database), no dependency installation beyond what the two proofs strictly need (aim for zero), no mocks, no tunnelling services, no writes outside `~/Desktop/SnapRecord`, no secrets in any committed file or in the report. Total model spend for this packet is capped at USD 0.50; stop and report if a single call exceeds USD 0.20.

## Stopping condition
If Proof A cannot deliver a photo after the one fallback, complete Proof B with any JPEG the user provides by other means (AirDrop, file copy), state plainly that Proof A FAILED with the symptom, and return the report. A failed proof recorded honestly is the deliverable; a proof skipped is not.

## Acceptance
1. Report quotes `pwd`, the listing, git state, runtime versions with actual output, LAN IP, and each credential as PRESENT/ABSENT with no values shown.
2. Proof A: the report shows the server log line for the upload with byte size and user-agent, the `sips` dimensions/orientation output, and a plain statement of whether `capture="environment"` opened the camera directly; or a FAILED statement with the exact symptom and the fallback tried.
3. Proof B: three raw responses are in evidence; for each, the report states validation pass/fail, token counts, elapsed ms, and USD cost with its source; the user's verbatim confirmation of `label_text` accuracy is quoted and labelled as the user's statement.
4. `schema/item.v0.json` exists byte-identical to the schema above; `ORIENTATION.md`, `RESULTS.md` (with ID scheme and collision check stated), the order and the report exist; contents are pasted in the report; a commit hash is reported; `git status` is clean after the commit.
5. The recommendation for the first product packet cites specific evidence from this packet for each of its three points (capture path, schema changes, model and cost per photo) and lists prerequisite user actions as plain sentences.
