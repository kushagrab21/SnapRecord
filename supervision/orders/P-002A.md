PACKET P-002A — Amendment to P-002 (supersedes instructions 3, 6, 7 and acceptance criteria 1 and 2 of P-002; everything else in P-002 stands)

**Project:** SnapRecord, `~/Desktop/SnapRecord`, branch `main`.
**Issued:** 2026-09-12 by the supervisor, on the Runner's report that ground truth is blocking and the user's statement that they cannot type it. **Target close:** 45 minutes from receipt.

## Supervisor rulings to record before work
4. **Ruling 2 is amended, not waived.** Ground truth must exist before extraction and must never be a model's or the Runner's reading of a photo. It MAY be known by construction: text the Runner generates, records with a timestamp, renders, and the user photographs. The three existing captures (AC medium, AC close, poster wall) have NO ground truth and are scored for consistency only; no accuracy claim may be made about them anywhere.
5. **Originals are preserved, always.** Every capture is stored at its exact original bytes under `data/captures/<sha256>.<ext>` with a sidecar `<sha256>.json` (original filename, bytes, width, height, content-type, user-agent, received-at). The file is never rewritten. The downscaled copy is a derivative stored as `data/derived/<sha256>.1568.jpg` and used for inference only. Every extraction result records BOTH the original and derived sha256. `data/` is git-ignored; the sidecars are copied into `supervision/evidence/` since they contain no pixels.
6. The user has confirmed, in this session, the three existing photos and the choice of second model. Record that as the user's statement; do not re-ask.

## Instructions (replacing P-002 §3, §6, §7)
3'. **Constructed ground truth.** Write `scripts/make-label.mjs` that generates `fixtures/label-001.json` — product name (two invented words), brand (one invented word), at least six numeric facts with units laid out as a nutrition-style table (energy, protein, fat, carbohydrate, sugar, sodium, plus net weight), and an ingredients sentence of at least twelve words — using invented but plausible values, seeded from the current timestamp so it cannot be predicted. Commit that file. Then render it as a dependency-free HTML page (black text on white, >= 28px, table layout, no images) served by `proof-capture.mjs` at `/label` and print one sentence asking the user to open it full-screen on the laptop and photograph the screen from the phone, in Safari, close enough that the whole table fills the frame. Store the capture per ruling 5. If moire or glare makes the text illegible to the human eye, ask for one retake; no more.
6'. **The matrix.** Four photos — three existing plus the label photo — x 2 models x 2 runs = 16 calls on the derived images, v1 schema, one fixed prompt pasted in evidence before call 1. Plus the label photo at full resolution through gemini-3.8-flash once (1 call) for the downscale comparison. Cap unchanged: USD 0.25 per call, USD 2.00 total.
7'. **Scoring.** `scripts/score.mjs` scores the label photo's responses against `fixtures/label-001.json` per fact (exact / normalised / missed as P-002 defined), covering the product name, brand, every numeric value with its unit, and the ingredients sentence. For the three unlabelled photos, `score.mjs` reports instead: v1 validation, `label_legible`, `capture.issues`, and **run-to-run agreement** — whether `item_name` and `category` are identical across the two runs of the same model. Output the 17-row table to `supervision/evidence/P-002-matrix.md`.

Instruction 8 (hallucination check) applies to the label photo only, and since ground truth is known by construction it is done in code: any attribute value not present in `fixtures/label-001.json` is listed as a fabrication. Quote the list; do not ask the user.

## Boundaries
As P-002, plus: nothing under `data/captures/` is ever opened for writing after creation; verify by re-hashing every original at the end of the packet and pasting the hashes.

## Acceptance (replacing P-002 criteria 1 and 2; 3, 4, 5 stand)
1'. `fixtures/label-001.json` is committed with a timestamp earlier than the label photo's received-at and the first extraction call; the report shows all three timestamps.
2'. The 17-row matrix is in evidence and the report; label-photo rows carry per-fact scores and the fabrication list; unlabelled rows carry consistency fields and NO accuracy score.
6'. Every original's sha256 at end of packet equals its sidecar's sha256, pasted; every extraction result in evidence names both the original and derived hash.
