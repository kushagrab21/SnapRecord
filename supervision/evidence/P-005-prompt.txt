You are extracting a structured record from a photograph of a physical object or its label.
Return ONLY a single JSON object, no markdown fence, no prose, matching exactly this JSON Schema:

{
  "$comment": "SnapRecord item record, v2. v1 unchanged plus required user_note (P-005). The note is stored verbatim (R-0024); audio bytes are content-addressed (R-0025).",
  "type": "object",
  "required": [
    "item_name",
    "category",
    "label_text",
    "attributes",
    "confidence",
    "field_confidence",
    "capture",
    "user_note"
  ],
  "properties": {
    "item_name": {
      "type": "string"
    },
    "brand": {
      "type": [
        "string",
        "null"
      ]
    },
    "category": {
      "type": "string"
    },
    "label_text": {
      "type": "string",
      "description": "all legible text on the label, verbatim"
    },
    "attributes": {
      "type": "array",
      "items": {
        "type": "object",
        "required": [
          "key",
          "value"
        ],
        "properties": {
          "key": {
            "type": "string"
          },
          "value": {
            "type": "string"
          },
          "unit": {
            "type": [
              "string",
              "null"
            ]
          }
        },
        "additionalProperties": false
      }
    },
    "quantity": {
      "type": [
        "number",
        "null"
      ]
    },
    "confidence": {
      "type": "number",
      "minimum": 0,
      "maximum": 1
    },
    "uncertain_fields": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "field_confidence": {
      "type": "object",
      "description": "one entry per top-level field the model filled; key is the field name, value is 0..1. Keys are closed to the v1 top-level content fields.",
      "propertyNames": {
        "enum": [
          "item_name",
          "brand",
          "category",
          "label_text",
          "attributes",
          "quantity"
        ]
      },
      "additionalProperties": {
        "type": "number",
        "minimum": 0,
        "maximum": 1
      }
    },
    "capture": {
      "type": "object",
      "required": [
        "label_legible",
        "issues"
      ],
      "properties": {
        "label_legible": {
          "type": "boolean"
        },
        "issues": {
          "type": "array",
          "items": {
            "type": "string",
            "description": "e.g. glare, blur, cropped, distance"
          }
        }
      },
      "additionalProperties": false
    },
    "user_note": {
      "type": "object",
      "description": "what the user said about this object, stored exactly as received and never altered by the model (R-0024)",
      "required": [
        "text",
        "source",
        "audio_sha256",
        "transcript_model"
      ],
      "properties": {
        "text": {
          "type": "string",
          "description": "verbatim; may be empty"
        },
        "source": {
          "enum": [
            "none",
            "dictation",
            "transcription"
          ]
        },
        "audio_sha256": {
          "type": [
            "string",
            "null"
          ],
          "description": "sha256 of the stored original audio at data/audio/<sha256>.<ext> (R-0025); null when no audio was recorded"
        },
        "transcript_model": {
          "type": [
            "string",
            "null"
          ],
          "description": "model that produced text when source is transcription; null otherwise"
        }
      },
      "additionalProperties": false
    }
  },
  "additionalProperties": false
}


Rules:
- label_text must contain ALL legible text on the label, verbatim, preserving line order; use \n between lines.
- attributes is an array of {key, value, unit} objects; unit may be null.
- confidence is your overall confidence 0..1.
- uncertain_fields lists the names of any fields you were unsure about.
- field_confidence gives one 0..1 score per top-level content field you filled (item_name, brand, category, label_text, attributes, quantity). Score each field on its own; do not repeat your overall confidence.
- capture.label_legible is true only if label text is actually readable in the image; capture.issues lists what degrades it, such as glare, blur, cropped, distance. Use an empty array if nothing does.
- Do not add any property not in the schema.
- Do NOT output user_note. The server fills that field with what the user said, verbatim; anything you put there is discarded.

The user said about this object: «Could you tell me what this thing is?». Use it to resolve ambiguity; never contradict what is visibly printed.
